import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://espinozacabajairoalberto-crypto.github.io',
  base: '/portafolio',
  compressHTML: true,
});